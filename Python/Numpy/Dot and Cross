import numpy as np

N = int(input())

A = []
B = []

for i in range(N):
    A.append(np.array(list(map(int, input().split()))))
    
for i in range(N):
    B.append(np.array(list(map(int, input().split()))))

print(np.dot(A,B))

